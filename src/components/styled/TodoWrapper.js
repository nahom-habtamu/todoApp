import styled from 'styled-components';

const TodoWrapper = styled.div`
    background:-webkit-linear-gradient(pink,lightblue,pink);
    height: 50%;
    width: 50%;
    border-radius: 50px;
    margin : auto;
    margin-top: 10px;
    text-align: center;
    min-width: 450px;
`;

export default TodoWrapper;